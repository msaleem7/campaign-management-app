<div class="container my-5">

    <div class="row ">
        <div class="col-8"><h2><%= @campaign.title.upcase%></h2>
        <%= link_to "🖋", :edit_campaign,id:"action_edit" if policy(@campaign).edit?%><%= link_to "🗑", :campaign,id:"action_delete", method: :delete,
                  data: { confirm: "Are you sure?" } if policy(@campaign).edit?%>
        </div>

        
    </div>

    <div class="row  mt-5">
        <div class="col">
             <h6 >Purpose: <%= @campaign.purpose.capitalize %></h6>
        </div>

        <div class="col">
            <h6>Created by: <%= @campaign.expert.username %></h6>    
        </div>

        <div class="col">
            <h6>Duration: <%= @campaign.estimated_duration %></h6>    
        </div>

        <div class="col">
            <strong><%= link_to 'DISCUSSIONS', campaign_topics_path(@campaign) ,id: "index_links" %></strong>
        </div>

    </div>

</div>


<div>




</div>

<%= render 'todos/show'%>

<div class="container my-5 pb-4">
<div class="row my-4 mx-1"><h5>Comments: <%= @campaign.comments.count %></h5></div>

<%= render @campaign.comments %>

<div class="row my-4 ">
    <%= render 'comments/form' , locals: {commentable: @campaign, type: "Campaign"}%>
</div>

</div>


